---
description: git 提交当前代码改动
---

## git commit

See `.opencode/skills/git-commit/SKILL.md` for commit format rules.

不要输出无用内容，只需要遵循如下步骤：

1. 理解 git-commit skill
2. 分析 git 代码改动
3. 执行 `pre-commit`
4. 按 skill 格式生成 commit message，执行 git commit 相关命令

即可，禁止 git push

## Usage

```
/commit
```
